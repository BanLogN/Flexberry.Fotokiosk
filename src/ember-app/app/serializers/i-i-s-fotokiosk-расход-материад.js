import { Serializer as РасходМатериадSerializer } from
  '../mixins/regenerated/serializers/i-i-s-fotokiosk-расход-материад';
import __ApplicationSerializer from './application';

export default __ApplicationSerializer.extend(РасходМатериадSerializer, {
  /**
  * Field name where object identifier is kept.
  */
  primaryKey: '__PrimaryKey'
});
